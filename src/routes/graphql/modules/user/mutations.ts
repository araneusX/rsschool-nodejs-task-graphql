import { FieldConfig } from "../../types/definitions.js";

export const createProfile: FieldConfig = {
  
};

export const changeProfile: FieldConfig = {

};

export const deleteProfile: FieldConfig = {

};
